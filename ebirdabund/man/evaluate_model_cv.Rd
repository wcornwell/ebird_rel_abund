% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{evaluate_model_cv}
\alias{evaluate_model_cv}
\title{Cross-validate a GAM on subsampled eBird data}
\usage{
evaluate_model_cv(df, formula = NULL, k = 5L, seed = 42L)
}
\arguments{
\item{df}{Data frame from \code{fit_species_model()$data} — subsampled, with
covariate columns already attached.}

\item{formula}{Optional model formula.  If \code{NULL}, built automatically via
\code{\link[=build_gam_formula]{build_gam_formula()}} (identical to \code{\link[=fit_gam]{fit_gam()}}).}

\item{k}{Number of CV folds.  Default \code{5}.}

\item{seed}{Random seed for reproducible fold assignment.  Default \code{42}.}
}
\value{
A list with:
\describe{
\item{fold_metrics}{Data frame with one row per fold: \code{spearman_r},
\code{pearson_r}, \code{mae}, \code{rmse}, \code{holdout_dev_expl}.}
\item{summary}{Named numeric vector: mean of each metric across folds.}
}
}
\description{
Runs k-fold cross-validation on the subsampled data frame returned by
\code{\link[=fit_species_model]{fit_species_model()}} and reports held-out prediction skill.  Pass a custom
\code{formula} to evaluate an alternative covariate set; leave it \code{NULL} to use
the same formula as \code{\link[=fit_gam]{fit_gam()}} (effort + all habitat covariates).
}
\examples{
\dontrun{
fit   <- fit_species_model(polygon, ebd, samp, "Superb Fairywren", cov)
cv    <- evaluate_model_cv(fit$data)
cv$summary

# Compare effort-only vs. full model
cmp <- compare_covariate_models(fit$data)
print(cmp)
}

}
