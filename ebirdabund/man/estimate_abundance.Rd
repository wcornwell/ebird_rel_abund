% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_abundance.R
\name{estimate_abundance}
\alias{estimate_abundance}
\title{Estimate relative abundance within a polygon}
\usage{
estimate_abundance(
  polygon,
  ebird_zip,
  sampling_txt,
  species,
  cov_stack = NULL,
  cache_dir = "ebirdabund_cache",
  grid_res_km = 1,
  hex_spacing_km = 5,
  peak_time = NULL,
  use_range = TRUE,
  range_resolution = "27km"
)
}
\arguments{
\item{polygon}{An \code{sf} object (POLYGON or MULTIPOLYGON) defining the
study area. Any CRS is accepted; it is reprojected internally.}

\item{ebird_zip}{Path to the raw eBird EBD \code{.zip} \emph{or} \code{.txt} file.}

\item{sampling_txt}{Path to the eBird sampling-events \code{.txt} file.}

\item{species}{Common name exactly as it appears in eBird
(e.g. \code{"Superb Fairywren"}).}

\item{cov_stack}{A \code{terra::SpatRaster} returned by \code{\link[=prepare_covariates]{prepare_covariates()}}.
If \code{NULL} (default), \code{\link[=prepare_covariates]{prepare_covariates()}} is called automatically using
\code{cache_dir}.}

\item{cache_dir}{Directory for the zero-filled eBird cache. Defaults to
\code{"ebirdabund_cache"} in the working directory.}

\item{grid_res_km}{Prediction grid resolution in km. Default \code{1}.}

\item{hex_spacing_km}{Hex-cell diameter for spatiotemporal subsampling in
km. Default \code{5}.}

\item{peak_time}{Optional decimal-hour override for observation start time
used in predictions. Estimated from the model when \code{NULL}.}

\item{use_range}{If \code{TRUE} (default), cells outside the eBird species range
are set to \code{NA}. See \code{\link[=predict_species_map]{predict_species_map()}} for details.}

\item{range_resolution}{Resolution for \code{ebirdst::load_ranges}; \code{"27km"}
(default) or \code{"9km"}.}
}
\value{
A named list with:
\describe{
\item{model}{The fitted \code{mgcv::gam} object.}
\item{predictions}{A \code{terra::SpatRaster} with layers \code{abd} and \code{abd_se}.}
\item{data}{The subsampled data.frame used for model fitting.}
\item{plot}{A \code{ggplot2} map of log-transformed relative abundance.}
}
}
\description{
Convenience wrapper that runs the full pipeline in one call by combining
\code{\link[=fit_species_model]{fit_species_model()}} and \code{\link[=predict_species_map]{predict_species_map()}}.  For more control — or to
reuse a fitted model across multiple prediction settings — call those two
functions directly.
}
\details{
It is strongly recommended to call \code{\link[=prepare_covariates]{prepare_covariates()}} first and pass the
result via \code{cov_stack}.  This separates the one-time raster download (which
can exceed 1 GB) from the per-species modelling.
}
\examples{
\dontrun{
library(sf)
library(ebirdabund)

nsw <- sf::st_as_sf(geodata::gadm("AUS", level = 1,
                                   path = "ebirdabund_cache")[
         geodata::gadm("AUS", level=1, path="ebirdabund_cache")$NAME_1
           == "New South Wales", ])

# Step 1 — once per region
cov <- prepare_covariates(nsw)

# Step 2 — modelling (once per species)
model_fit <- fit_species_model(
  polygon      = nsw,
  ebird_zip    = "raw_data/ebd_AU-NSW.txt",
  sampling_txt = "raw_data/ebd_AU-NSW_sampling.txt",
  species      = "Superb Fairywren",
  cov_stack    = cov
)

# Step 3 — prediction (masked to the species range)
result <- predict_species_map(
  model_fit,
  polygon = nsw,
  species = "Superb Fairywren"
)
result$plot
}

}
