% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_abundance.R
\name{predict_species_map}
\alias{predict_species_map}
\title{Predict abundance from a fitted model}
\usage{
predict_species_map(
  model_fit,
  polygon,
  species,
  sci_name = NULL,
  grid_res_km = 1,
  peak_time = NULL,
  use_range = TRUE,
  botw_path = NULL,
  range_resolution = "9km"
)
}
\arguments{
\item{model_fit}{The list returned by \code{\link[=fit_species_model]{fit_species_model()}}.}

\item{polygon}{An \code{sf} object defining the study area (used for the initial
polygon mask and the plot extent).}

\item{species}{Common name used for range lookup and the plot title.}

\item{grid_res_km}{Prediction grid resolution in km. Default \code{1}.}

\item{peak_time}{Optional decimal-hour override for observation start time
used in predictions. Defaults to the median observation start time in the
training data (a "typical checklist" reference).}

\item{use_range}{If \code{TRUE} (default), cells outside the eBird species range
(from \code{ebirdst::load_ranges}) are set to \code{NA}.  Falls back to polygon-only
masking with a warning if the range cannot be loaded.}

\item{range_resolution}{Resolution passed to \code{ebirdst::load_ranges};
\code{"27km"} (default) or \code{"9km"}.}
}
\value{
A list with:
\describe{
\item{predictions}{A \code{terra::SpatRaster} with layers \code{abd} and \code{abd_se}.}
\item{plot}{A \code{ggplot2} map of log-transformed relative abundance.}
}
}
\description{
Generates a gridded abundance surface from the output of
\code{\link[=fit_species_model]{fit_species_model()}}, optionally masking cells outside the eBird species
range.
}
\details{
Range polygons are obtained via \code{ebirdst::load_ranges()} (requires the
\strong{ebirdst} package and a downloaded species dataset).  All season polygons
are unioned into a single presence mask before cells are set to \code{NA}.  Set
\code{use_range = FALSE} to skip this step, or if \strong{ebirdst} is not available.
}
\seealso{
\code{\link[=fit_species_model]{fit_species_model()}}, \code{\link[=estimate_abundance]{estimate_abundance()}}
}
