% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{compare_covariate_models}
\alias{compare_covariate_models}
\title{Compare effort-only vs. full habitat model via cross-validation}
\usage{
compare_covariate_models(df, k = 5L, seed = 42L, ...)
}
\arguments{
\item{df}{Data frame from \code{fit_species_model()$data}.}

\item{k}{Number of CV folds.  Default \code{5}.}

\item{seed}{Random seed.  Default \code{42}.}

\item{...}{Named formulas for additional model variants to compare.}
}
\value{
A data frame with one row per model variant and columns for each
cross-validated metric.
}
\description{
Convenience wrapper around \code{\link[=evaluate_model_cv]{evaluate_model_cv()}} that fits two formulas on
the same data and returns a side-by-side comparison:
\itemize{
\item \strong{effort_only} — day-of-year, time, duration, distance,
observers, protocol (no habitat covariates).
\item \strong{full} — effort terms + all habitat covariate columns
(names starting with \code{lc_}, \code{elevation}, \code{precip_}, or \code{temp_}).
}
}
\details{
Additional named formulas can be passed via \code{...} to evaluate custom
covariate sets.
}
